- name: Run apt update to update sources list
  apt:
    update_cache: yes
  
- name: Install required packages
  apt:
    pkg:
      - gnupg 
      - software-properties-common 
      - curl
      - python3-pip
      - golang
    state: present